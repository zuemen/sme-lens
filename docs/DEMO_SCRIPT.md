# 決賽 Demo 演示腳本 — 50 萬 USDT 出金攔阻情境

> 對應提案書第一章招牌情境。建議演示時間：2.5–3 分鐘（嵌入 8 分鐘簡報的中段）。
>
> **兩種演示方式**：
> - **網站版（預設）**：全程在瀏覽器完成，不需要開終端機或本機 Streamlit。見下方「網站版演示前準備」。
> - **Streamlit 本機版（備用）**：全程離線可跑（內建劇本圖），現場無網路也不炸。見「Streamlit 本機版演示前準備」。
>
> 以下「演示動線」的步驟兩版通用（畫面內容一致），差別只在開場的準備動作。

## 網站版演示前準備（預設）

- 網址：`<部署後填入，見 README「部署 → Demo 網站（web/）」>`
- 開場前至少 30 秒先打開網址跑一次「執行出金審查」（喚醒 Vercel serverless 冷啟動，
  實測約 5 秒），現場按鈕即可秒出結果
- **離線保險網的實際涵蓋範圍**（如實記錄，不是「全程離線可跑」）：只有「出金審查」頁、
  目標地址為 `TOtcOut01`、且失敗原因是**完全無法連線**（斷網/DNS/CORS 被擋）或**後端
  5xx**（Vercel 冷啟動逾時、API 500 等）時，才會自動退回內建離線快照並明確標示
  「離線快照」字樣（快照金額固定為 500,000 USDT）。以下情況**沒有**安全網，只會顯示
  一行錯誤訊息：`VITE_API_BASE` 設錯導致的 404/405、對照組 `TNormalUser01`、
  「金流圖譜工作台」頁的任何查詢。演示對照組（第 5 步）前務必先確認 API 連線正常。
- 若中途誤觸瀏覽器重新整理或直接輸入子頁網址（例如 `/screening`），部署方已設定
  SPA rewrite（`web/vercel.json`），應正常回到同一頁，不會 404。

## Streamlit 本機版演示前準備（備用，全程離線可跑）

```bash
make app   # 或 streamlit run smelens/app/workbench.py
```

- 模式選「出金審查 Demo（50 萬 USDT 攔阻情境）」
- 事先跑一次確認瀏覽器快取熱好，現場按鈕即出結果
- 本版本內建劇本圖，現場無網路也不炸

## 演示動線（口白 ＋ 操作）

### 第 1 步：拋出問題（口白，約 30 秒）

> 「某交易所用戶申請將 50 萬 USDT 提領到這個外部地址。這個地址**從來沒被通報過、
> 不在任何黑名單上**。傳統法遵工具比對名單——放行。錢一出去，就跨境追不回來了。」

### 第 2 步：執行審查（操作，約 20 秒）

- 目標地址保持預設 `TOtcOut01`、金額 500,000 → 按「執行出金審查」
- 指著決策卡念出：**綜合風險 0.66 → 加強審查（EDD）**

> 「注意三個分數：它**自身**結構分數只有 0.15——名單與單點特徵都攔不住它，
> 這個地址本身乾乾淨淨，沒命中任何圖樣、也不在任何黑名單上。真正拉高風險的是
> **關聯分數 0.60**：它的上游兩步之內，就是一個命中『集資扇入＋快速分散＋集散』
> 三種洗錢圖樣的集資主錢包。正因為自身結構真的沒有異常，我們給的不是硬擋，
> 而是加強審查——把整段二階關聯證據交給法遵人員覆核，這是比一律封殺更站得住腳、
> 也更好向監理單位交代的處置。」

### 第 3 步：圖譜說故事（操作，約 60 秒）

- 捲到金流圖譜，指出橘色高亮路徑：`TAggregator01 → TMule03 → TOtcOut01`
- 由左至右講一次完整劇本：

> 「左邊 12 個被害人，在『限時加碼』話術下 50 分鐘內集中入金到 3 個客服收款地址
> ——這是 fan-in 集資。資金歸集到主錢包後，30 分鐘內拆給 6 個車手——fan-out。
> 車手再走剝洋蔥鏈層層轉手，最後整合到這個 OTC 出金地址。
> 集資—分層—整合，教科書級的洗錢三階段，我們的圖樣庫**主動掃出來**的，
> 不需要任何黑名單。」

### 第 4 步：STR 草稿收尾（操作，約 40 秒）

- 展開「可疑交易申報（STR）草稿」，捲動展示證據鏈

> 「而且每一筆判定都有可稽核的證據：哪個風險節點、幾階關聯、路徑上每一跳的
> 金額和時間戳。法遵人員審閱後可直接轉為申報文件——這就是我們說的
> 『調查敘事導向』，不是黑箱分數。」

- 點「下載 STR 草稿」展示一鍵產出

### 第 5 步：對照組防守（備用，約 20 秒）

評審若質疑誤報率，切換目標為 `TNormalUser01`（正常用戶）再按一次：

> 「同一套引擎，正常地址風險 0.10，放行。結構偵測不是寧可錯殺。」

## 預期 QA 與回應要點

| 可能提問 | 回應要點 |
|---|---|
| 資料是真的嗎？ | 劇本圖為合成資料，比例與時間節奏參照 165 公開案例之假投資出金模式；工作台另有「金流圖譜工作台」模式可抓 TRON 主網真實地址 2-hop 圖 |
| 閾值怎麼定？ | 0.7/0.4 為 Demo 預設；正式部署依業者風險胃納與歷史誤報率校準，並支援分層處置 |
| 和 GNN 的關係？ | 本 Demo 展示規則＋關聯層；GNN 分數以 noisy-or 併入同一融合公式（`screening.py`），Elliptic 基準見 RESEARCH.md |
| 誤報怎麼辦？ | 決策採分級處置（加強審查 EDD／暫緩並人工審查），不是一律直接拒絕；本案自身結構無異常，給的是 EDD——STR 草稿即為人工審查的起點，人在迴路中 |
